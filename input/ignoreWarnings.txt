== Suppressed Messages ==
# The group source "codes" are ObservationDefinition resource ids (UUIDs), so there is no source code 'system' url to include
%No Source Code System, so the source codes cannot be checked

# These extensions do not appear in the ConceptMap instances and cannot be fixed in the IG
EXTENSION_EXTM_CONTEXT_WRONG_XVER

# These are example manufacturer local code systems that are used in only example instances, so they are not expected to be known to the IG Publisher.
%Code System URI 'http://www.corelaboratory.com/ARCHITECT' could not be found so the code cannot be validated
%Code System URI 'http://www.corelaboratory.abbott/ARCHITECT' could not be found so the code cannot be validated
%Code System URI 'http://ww.roche.com/cobas8000' could not be found so the code cannot be validated

# The LOINC part (LP) codes are not currently supported on tx.fhir.org.  The IG Publisher should be recognizing and using the loinc-fragment-livd CodeSystem resource, which is included in the IG and does contain those 'LP' codes, but that is not happening.
%Unknown Code 'LP%

# The SNOMED CT code system that is bundled in the IG is a fragment, so it is not expected to contain all SNOMED CT codes - only the ones that are needed in this LIVD bundle instance.
%Target Code System http://snomed.info/sct doesn't have all content (content = fragment), so the target codes cannot be checked
